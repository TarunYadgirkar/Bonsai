---
description: Show this project's Bonsai conversation tree — branches, routing, pruning economics, merged insights.
---

Show the Bonsai tree for this project: invoke the `tree` skill from the bonsai plugin (it calls
`bonsai_tree` with this project's cwd and renders the preformatted tree, totals footer included).
