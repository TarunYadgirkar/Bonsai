---
description: Fork a side question into a Bonsai branch with a compiled minimal brief, auto-routed to the right model + effort.
argument-hint: <the side question>
---

Fork this side question into a Bonsai branch: $ARGUMENTS

Invoke the `branch` skill from the bonsai plugin and follow it exactly: compile the ≤8-fact
referent-resolved brief from this conversation, register the fork with `bonsai_fork`, respect
the coverage gate, spawn the routed subagent, and merge exactly one distilled insight back with
`bonsai_merge`. If no question was given, ask for one — never fork blind.
