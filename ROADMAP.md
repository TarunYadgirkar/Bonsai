# ROADMAP.md — the public-product campaign

Synthesized 2026-08-19 from an 11-scout landscape sweep (branching OSS, native-feature
sherlock risk, memory/compression, model routing, MCP ecosystem, Claude Code plugins,
cost observability, tree-viz UX, OSS launch playbooks, repo self-audit). 111 items.
Living document — check items off, reprioritize freely; the record stays in BUILDLOG.md.

## Competitive posture

- Branching is fully commoditized: ChatGPT, Gemini, Grok, LibreChat, Open WebUI, Lobe, TypingMind all ship copy-forks; HN explicitly calls it table stakes.
- Every vendor fork copies full history because vendors bill by the token — an adversarial incentive that makes compiled minimal briefs Bonsai's durably unclaimed lane.
- Merge-back exists nowhere shipped (only 36-star forky and the unassigned claude-code #32631 spec); sherlock risk is highest in Claude Code within ~12 months (tree view + summary merge are spec'd; briefs and routing are not).
- Routing is validated and hot (RouteLLM, Not Diamond, OpenRouter, FrugalGPT), but the GPT-5 silent-router backlash proved readable reasons + never-override-manual-picks is the trust posture nobody currently claims.
- Compression-at-fork-boundary beats compression-at-buffer-overflow, with public evidence: Claude auto-compact complaints, context-rot research, and ContextBranch's 58% reduction / +13% on distant explorations.
- Tree UIs never mainstream as primary surfaces (Loom, Flowith, canvas apps); linear-read pane + tree-as-map is the surviving pattern, and the map must ship with fork, never retrofitted.
- Economics virality mechanics are proven by ccusage/viberank: local, auditable, screenshot-shaped counterfactual numbers spread; the savings-leaderboard inversion of tokenmaxxing is unclaimed.
- Distribution reality today: no Bonsai surface is usable by a stranger — broken plugin install, operator-manual connector keys, unpublished npm package, mock-only public demo.
- Credibility rests on the benchmark becoming public, named, and third-party-runnable with a full-history baseline number, because the field's self-reported benchmarks are reflexively distrusted.
- Posture: race the sherlock clock — fix distribution now, occupy the fork/merge/tree vocabulary, publish auditable receipts, and deepen the three moats (brief compiler, routing flywheel, one-insight merge) before native tools ship the shallow versions.

## P0 — ship blockers for public use

_A stranger cannot fully use Bonsai until these land._

- [x] **Promote copy-a to main, make the repo public, fix the two-command plugin install** — The documented install fails today and Vercel deploys main only — nothing else matters until a stranger can install from a clean session. `growth` **(done: main promoted 2026-08-19, old main archived as main-pre-phase4, repo PUBLIC, Neon main migrated 001-005 additive, bonsai-lac serving, CI green; npm publish is the one remaining piece — Tarun's)** `small`
- [x] **Self-serve connector key provisioning: /connect page mints a rate-limited garden key and hands back the ready connector URL** — Today no stranger can use the claude.ai surface without an operator SQL INSERT — smallest change that makes the flagship surface adoptable. `connector` `medium` **(done: shipped 2026-08-19 — live at /connect, verified on prod)**
- [x] **Resolve the npm name (bonsai-engine — verified free) and publish 0.1.0** — The @bonsai scope is likely unclaimable and a post-launch rename burns every quick-start link; the 'publishable engine' claim is unverifiable until the package exists. `engine` **(done: renamed to bonsai-engine repo-wide, pack verified; `npm publish` awaits Tarun login or the v* tag workflow)** `small`
- [~] **Set SESSION_SECRET in prod and move rate limiting to a Neon-backed sliding window** — Closes both documented security residuals — the population-prior Sybil cap and the per-serverless-instance rate-limit caveat. `web-app` `small` _(partial: signing code + in-memory limiter shipped; prod env var + Neon-backed window still open)_
- [ ] **60-second first-session value: preloaded example trunk so a visitor can fork→route→merge and watch the ledger move, no signup, no key** — 'Didn't see value fast enough' is the top devtool abandonment reason and first-session success is the 60%-vs-8% retention cliff. `web-app` `medium`
- [x] **README launch pass: delete the cut Chrome-extension row, add CI/npm/license/eval badges, copy-pasteable quick-start in the first 200 words** — A stale surface table is a credibility hit and quick-start-first is the strongest measured README converter. `docs` **(done: badges, 60-second try-it block, extension row replaced with parked note)** `small`
- [x] **Fail-loud pricing: unknown model ids show 'unpriced' and are excluded from savings totals; version and stamp the price table** — LiteLLM's silently-wrong costs destroyed trust — a savings number that is sometimes wrong is worse than none, and honest accounting is the stated moat. `engine` **(done: isPricedServedBy + unpriced rows excluded from all $ totals; PRICES_AS_OF surfaced)** `small`
- [x] **Add readOnlyHint/destructiveHint annotations and titles to all four MCP tools** — Mandatory for Connectors Directory review and a 15-minute change. `connector` `small` **(done: already present — all four tools carry titles + full annotation sets)**
- [x] **CI release workflow: npm Trusted Publishing with --provenance, publint + attw checks, gated on the eval suite passing** — Provenance badge plus a green eval gate is visible supply-chain credibility for a context-handling tool, and CI is the only way to get it. `engine` **(done: .github/workflows/release.yml — provenance, eval-gated, publint+attw)** `small`
- [x] **Wire mcp/smoke.mjs and 'claude plugin validate' into GitHub Actions on push to main** — A broken dist/server.mjs or manifest ships silently otherwise; directory screening and users expect green CI. `plugin` **(done: plugin validate + smoke + dist-sync all green in CI)** `small`

## P1 — differentiators (the amazing tier)

_The moat-deepening work: briefs visible, merges auditable, savings shareable._

- [x] **Per-fork savings receipt: two-number before/after pair ('1.8k sent · 41k avoided · $0.31') on every branch node, merge summary, and MCP tool result** — Helicone-style per-event attribution makes the aggregate a sum of auditable rows, and every viral savings story is a concrete pair, not a percentage. `web-app` `medium` **(done: sent/avoided pairs in chat header, connector fork line, MCP Apps view)**
- [x] **Counterfactual 'ceiling cost' column in the economics ledger: what each turn would have cost on the ceiling model with full history** — Computable exactly from token counts with zero extra API calls, and it's the number no vendor will ever show because full-history branches earn them revenue. `engine` **(done: ceilingCostUsd column in the ledger)** `small`
- [~] **Brief preview sheet on fork: compiled facts as deletable/pinnable chips, anchors highlighted, token count and tokens-saved, edits feed salience as training signal** — The brief is the moat but is invisible; showing and letting users correct it sells the pitch at the moment of value and converts errors into labeled data. `web-app` `medium` _(partial: sheet shipped — chips, pinned anchor, live receipt, server-side curateBrief with anchor re-pin; edits-as-salience-training still open)_
- [~] **Select-text-to-branch: highlight any span in a response, press B to fork with that span auto-pinned as the brief's primary anchor fact** — The most-requested fork gesture across competitors (Open WebUI #17086, Msty Delve) and it feeds the compiler a high-salience seed for free. `web-app` `medium` _(partial: web app has select-to-branch with anchor scoping; the B hotkey + explicit pin UI still open)_
- [ ] **Merge chip in parent: render the distilled insight as an inline expandable chip in the parent transcript, linking back to the full branch** — Makes the one-insight merge contract visible and auditable — no other tool renders where merged knowledge entered the parent. `web-app` `medium`
- [x] **Merge staleness/conflict check: diff parent messages added since fork against the insight; warn and offer re-ground instead of silently merging** — forky and ContextBranch both validate divergence detection — a stale insight merged blindly poisons the parent context Bonsai promises to keep clean. `engine` **(done: parentDriftTurns disclosed on merge with a reground nudge)** `medium`
- [x] **Distilled-insight faithfulness check: verify the insight's entity tokens appear in the branch transcript before writing it to the parent** — The merge contract currently trusts the distiller; a hallucinated insight poisons the parent permanently and the check is cheap. `engine` **(done: insightGroundedIn gate + corrective retry + extractive fallback)** `small`
- [x] **Connector OAuth 2.1 + DCR (Better Auth MCP plugin): PRM well-knowns, /register, PKCE, withMcpAuth — with the legacy /api/mcp/[key] path kept as an authless alias** — The whole 'strangers attach with zero setup' requirement, a hard prerequisite for directory listing, and it removes the key-in-URL exposure without breaking existing installs. `connector` **(done: hand-rolled minimal AS — PRM/AS well-knowns, DCR, PKCE-S256-only, hashed opaque tokens onto garden keys, consent page rides the /connect session; full DCR→token→MCP flow verified against the real DB; legacy key path untouched)** `large`
- [ ] **Replace plugin/mcp/server.mjs hand-mirror with an esbuild bundle of the real bonsai-engine** — Kills the documented dual-maintenance drift trap and gives the declared primary surface the real salience compiler and router instead of a subset clone. `plugin` `medium`
- [ ] **Plugin slash commands occupying issue #32631's vocabulary: /bonsai:branch, /bonsai:tree, /bonsai:switch, /bonsai:diff** — The community spec is unassigned with no PRs — being the reference implementation of the exact command names users are asking for is discoverability plus free positioning. `plugin` `medium`
- [ ] **UserPromptSubmit hook that detects side-question phrasing ('btw', 'quick question', 'tangent') and nudges the branch skill** — Superpowers' 752k installs prove enforcement is everything — Bonsai's value only materializes if branching fires on natural phrasing without the user naming the plugin. `plugin` `medium`
- [ ] **/compact-alternative in the plugin: at high context usage, offer 'fork a Bonsai branch with a compiled brief' instead of waiting for auto-compact** — Auto-compact complaints (issue #13112) are documented pain, and fork-at-a-semantic-boundary is exactly Bonsai's answer on a surface it already occupies. `plugin` `medium`
- [ ] **Plugin-side learning: persist per-user routing priors in plugin data, record overrides/escalations, route future branches through adjustForProfile** — MOAT.md's flywheel currently collects zero data on the primary surface — this puts the switching-cost mechanism where the product bet is. `plugin` `large`
- [ ] **Typed pinned slots in the brief compiler: {decisions, constraints, entities, quantities} exempt from salience pruning, Letta-core-memory style** — Single anchor-fact pinning already fixed one depth-3 bug; multi-slot pinning prevents the same bug class for constraints and decisions. `engine` `medium`
- [ ] **Verbatim anchor mode: pin exact quoted excerpts (code, numbers, constraints) unsummarized, and carry the forked exchange as a raw quote block** — HN's explicit wish was 'auto-compact that extracts excerpts without summarizing' — paraphrase is where identifiers and referents die. `engine` `medium`
- [ ] **No-recompression invariant: compileBrief never re-summarizes an inherited brief, only composes and pins — with a byte-identity test** — Summaries-of-summaries compounding is the top documented auto-compact failure and Bonsai's composition chain hits the same risk at depth 3+. `engine` `small`
- [ ] **Fact supersession: when extracted facts share entities but conflict, keep the later-turn version marked 'updated from X' (Zep validity-intervals lite)** — Salience can currently pin a stale fact after the trunk corrects it — the knowledge-update failure LongMemEval shows everyone fails. `engine` `medium`
- [ ] **FrugalGPT-style answer-quality gate: cheap self-check after a quick-tier answer, auto-escalate below threshold instead of waiting for user retry** — Turns the escalation ladder from user-triggered to automatic and removes the worst failure mode — the user receiving a bad cheap answer. `engine` `medium`
- [ ] **Typed escalation failure modes: refusal escalates model, truncation escalates effort, 'missing context' widens the brief, wrong-answer escalates tier** — One-size-fits-all escalation wastes the context-first lever Bonsai uniquely has; typed failures pick the cheapest corrective lever. `engine` `medium`
- [ ] **Routing reason as a first-class UI moment: RoutingChip expands to kind, confidence, which prior moved the decision, plus 'should have been bigger/smaller' feedback** — The GPT-5 silent-router backlash proved opaque routing destroys trust, and the feedback pair is the cheapest labeled training signal for the flywheel. `web-app` `medium`
- [ ] **Single routing-aggressiveness dial: thrifty↔premium slider shifting tier thresholds, persisted per user** — RouteLLM proved one scalar knob is the right cost/quality UX while learned per-kind priors stay underneath. `web-app` `small`
- [ ] **Benchmark full-history baseline arm: same questions answered from the complete transcript, reporting fidelity delta and token cost per case** — 'X% of full-copy fidelity at Y% of the tokens' is the one number the entire pitch rests on and the first question every HN commenter will ask. `engine` `large`
- [ ] **Name and publish the benchmark (BriefBench): results table with headline numbers, stranger-runnable npx one-liner, provider adapters, invitation to run other tools** — MOAT.md calls owning this benchmark moat #2; today it's invisible repo markdown, and self-benchmarks without a third-party run path read as assertions. `growth` `medium`
- [ ] **Topical-distance gate in the compiler: score branch-question distance from parent and widen the brief toward fuller context when distance is low** — ContextBranch shows minimal-context branching wins big on distant explorations (+13%) and does nothing on close ones — a fixed-size brief underperforms near-topic. `engine` `medium`
- [ ] **Tree-scoped anchor facts (Grok-workspace-style): pinned facts/instructions on a tree root that every branch's brief always includes** — Per-workspace memory is Grok's stickiest feature; tree-scoped pins give the same continuity while keeping briefs minimal. `engine` `medium`
- [ ] **MCP Apps interactive garden: clickable nodes calling bonsai_merge/bonsai_abandon through the ext-apps bridge, plus a per-branch 'copy brief' action** — Launch-partner apps established in-iframe action buttons as the idiom; it turns the garden from a picture into the actual fork/merge control surface. `connector` `medium`
- [ ] **MCP Apps economics chart view: per-branch spend vs full-history counterfactual with a cumulative savings line as a ui:// resource** — Makes 'the cheapest token is the one you don't send' visible inside claude.ai where the user forks, following the proven Hex/Amplitude pattern. `connector` `medium`
- [ ] **Tree legibility past 30 nodes: DOITree collapse-to-badge elision ('+12 branches · $X saved') plus focus-follows-conversation auto-expand/collapse** — Obsidian's lesson is local-graph-beats-hairball, and elision badges double as an economics surface; the tree must answer 'where am I', not 'show me everything'. `web-app` `medium`
- [ ] **Encode state and cost on every tree node: colored state rail, token-heat tint, routed-tier glyph** — Status-free graph nodes are Obsidian users' top complaint, and Bonsai's routing/economics data belongs in the map, not only the ledger. `web-app` `small`
- [ ] **One-click branch pruning: abandon-with-reason tombstones plus a 'clean up merged/dead branches' sweep with undo** — Junk-branch accumulation is the top real-world complaint against every shipped branching UI (Open WebUI #8928, ChatGPT, HuggingChat). `web-app` `small`
- [ ] **Comparison table at the top of README, landing, and marketplace listing: Bonsai vs /btw vs /fork vs ChatGPT branch vs LibreChat (context carried, routing, merge-back, tree, cost)** — Native /btw and /fork are the first objection every 2026 user raises — /btw has no merge and full context, /fork copies everything; the table is the pitch. `docs` `small`
- [ ] **Adversarial-incentive positioning rewrite: 'vendors copy your whole history because they bill you for it; Bonsai compiles a brief'** — Branch-as-copy is table stakes everywhere; the incentive framing is the sharpest one-sentence differentiation available and is verifiably true. `growth` `small`
- [ ] **Landing route restructure to converting order: 3-second hero with animated brief-compile, problem-oriented feature blocks, metrics trust block, FAQ, loud CTA** — The demo currently opens into the app; a 3-second-legible hero is the difference between a bounce and a fork. `web-app` `medium`
- [ ] **VHS-scripted demo GIFs per surface (fork→route→merge with the token counter), CI-regenerated, above the fold in both READMEs** — 62% of top-100 repos lead with motion and the loop is inherently animated; a .tape script never goes stale. `docs` `medium`
- [ ] **Show HN launch package prepared in advance: literal title, repo link, founder first-comment with the depth-3-bug-found-by-our-own-benchmark honesty beat; staggered per-surface launches after** — Prepared copy is what makes the first-60-minutes response window survivable, and repeat per-surface launches (engine, connector, plugin) is the proven pattern. `growth` `small`
- [ ] **Methodology page plus inline '(how is this computed?)' links showing the exact baseline formula and raw token counts behind every savings figure** — Cost-avoidance numbers are reflexively doubted; a stated, clickable formula separates credible from marketing. `docs` `small`
- [ ] **Connectors Directory submission package: help-center docs, seeded reviewer test account, drafted policy acknowledgments** — Directory review gates on docs and reviewer access, not code — start the non-code work now for the main distribution channel. `growth` `medium`
- [ ] **Auto-name branches from the compiled brief's topic phrase, shown in tree, tabs, and ledger** — Users can't find where branches happened in every competitor; the compiler already knows the topic so navigation labels are free. `engine` `small`

## P2 — polish

_Quality, trust, and finish._

- [ ] **Escape-hatch fork scope: default compiled brief with an 'include full history' option and remember-my-choice** — A trust ramp for users burned by lossy context, and per-user scope choices are router training signal. `web-app` `small`
- [ ] **30-second interactive fork/merge walkthrough on first launch (scripted conversation, guided branch, guided merge, ledger reveal)** — LibreChat's #1 fork complaint is comprehension, not capability; Bonsai's semantics are stranger and need show-don't-tell. `growth` `medium`
- [ ] **Cross-branch full-text search filtering the tree in real time, with jump-to-node** — Explicit unmet request from ChatGPT branching users and SillyTavern Timelines' best-in-class affordance that no vendor ships. `web-app` `medium`
- [ ] **Keyboard-first tree navigation wired into the command palette: arrows walk the tree, Enter switches transcript, f forks, m merges** — The Loom lineage proves power users — Bonsai's early adopters — live on keybindings, and the palette plumbing exists. `web-app` `small`
- [ ] **Stable per-branch hue (branch-id hash to OKLCH ramp) across sidebar rail, edge, transcript header, and ledger** — GitKraken's lane coloring is what makes parallel branches scannable; cross-surface color continuity is cheap and high-payoff. `web-app` `small`
- [ ] **Organic garden animations: new branches grow in via stroke-dashoffset spine draw; merged insights travel the child-to-parent edge as a pill** — Positions are already pure data so this is cheap, and one or two well-placed animations carry the entire garden metaphor at demo time. `web-app` `small`
- [ ] **Fix deep-chain unreadability: cap visual indent at depth 4 with a breadcrumb strip instead of narrowing to 148px** — Depth-6 cards are currently unreadable; git-graph lanes prove indent doesn't need to grow forever. `web-app` `small`
- [ ] **Row-density toggle: GitKraken-style 28px compact rows via a second height table in the same layout pass** — One component then serves both the 10-node demo and a 200-node power-user tree without a rewrite. `web-app` `small`
- [ ] **Hover peek popover on tree nodes: brief excerpt, pinned anchors, merged insight; click pins, Esc dismisses** — Loom's map/document split — users need to preview a branch without losing their place in the transcript. `web-app` `medium`
- [ ] **Abandoned-branch composting: faded outline dots collapsing to '+N composted', resurrectable via palette toggle** — Keeps the garden metaphor honest (pruning is a feature) and stops dead explorations consuming layout space. `web-app` `small`
- [ ] **Garden minimap over ~40 nodes: dot-scale overview strip with draggable viewport, hidden below threshold** — Standard focus+context aid for when elision alone stops being enough, without cluttering small trees. `web-app` `medium`
- [ ] **Level-of-detail for the MCP Apps inline garden: card/chip/dot variants by width and node count, active path always full detail** — tldraw's LOD pattern adapted to the inline view's hard width constraint; canvas UIs die on small surfaces without it. `connector` `medium`
- [ ] **Engine-side computeGardenLayout(tree, focusId, density) returning positioned nodes/edges with LOD tiers, shared by web sidebar and MCP Apps view** — One dependency-free layout function keeps the two surfaces pixel-consistent and unit-testable instead of parallel layout logic. `engine` `medium`
- [ ] **Per-turn effort routing inside a branch with escalation decay, instead of one decision per branch** — Not Diamond Code's per-step routing claims 20%+ savings; a branch that escalated once shouldn't stay pinned high for trivial follow-ups. `engine` `medium`
- [ ] **Cache-aware routing: weight decisions toward the model with a warm prompt cache in costForModel** — Cache-read pricing at ~10% often inverts the cheap-model choice; honest cost accounting must include it. `engine` `medium`
- [ ] **Cache-aware baseline honesty: discount the counterfactual to cache-read price for the cached prefix, and say so in the methodology note** — Savvy developers will immediately object that full history would be cache-priced; pre-empting the strongest objection is what survives Hacker News. `engine` `medium`
- [ ] **Trailing 7-day recency weighting on community priors (OpenRouter-style) instead of all-time aggregation** — Model quality and pricing shift fast; stale all-time priors mis-route after every model release. `engine` `small`
- [ ] **Entity linking in the compiler: per-path alias map so 'GRFP'/'the fellowship'/'NSF GRFP' count as one entity in salience and anchor carry-through** — mem0's one clearly-working idea; anchorCarriedThrough currently matches capitalized tokens literally and misses rephrasings. `engine` `medium`
- [ ] **Routing profile as inspectable memory: settings pane listing learned per-kind tier preferences with delete/reset** — Bonsai does have a durable memory layer (the flywheel); ChatGPT memory's top criticism is opacity, so inspectability is cheap differentiation. `web-app` `small`
- [ ] **Per-branch routing history panel surfacing inference_logs per node, fixing the documented 'chip shows only the last turn' honesty trap** — The economics pitch depends on the UI never misrepresenting routing; the data is already stored, only surfacing is missing. `web-app` `small`
- [ ] **Cost-in-the-response: usage {inputTokens, outputTokens, costUsd, baselineCostUsd} on every chat/fork/merge API and MCP result, faint per-message cost chip in UI** — OpenRouter proved developers trust cost delivered with the completion far more than a dashboard reconstructed later. `engine` `small`
- [ ] **Cumulative all-time ledger: lifetime aggregate ('142k tokens pruned across 37 branches since June') with a per-day savings sparkline** — Session-scoped numbers are small; lifetime numbers get big enough to brag about — the ccusage virality mechanic. `web-app` `medium`
- [ ] **Router savings attribution: split ledger savings into 'brief compilation' vs 'downcascade routing', each with its own counterfactual line** — Bonsai has two savings mechanisms; blending them hides the routing flywheel's value and weakens the per-mechanism story. `engine` `medium`
- [ ] **CSV/JSON ledger export: one row per inference with actual and baseline token counts and the price-table version used** — Auditable raw rows are the ultimate credibility artifact for skeptical teams, and exportable period totals are a repeatedly-asked-for gap in Langfuse/OpenRouter. `web-app` `small`
- [ ] **LongMemEval-taxonomy eval cases: knowledge-update, abstention (expect covered:false), and temporal reasoning** — Maps the benchmark onto the field's accepted five-ability taxonomy, exposing the two abilities it doesn't yet test. `engine` `small`
- [ ] **LLM-judge grounding tier in the benchmark: extract claims from brief-based answers and score faithfulness against the full trunk** — Entity-presence checks miss paraphrase-level distortion; grounding is the compression-quality metric the LLMLingua literature converged on. `engine` `medium`
- [ ] **Nightly live-graded eval: scheduled CI runs the eval against a real Anthropic key and publishes pass numbers as a dated badge** — The benchmark's differentiator is that the same assertions grade live models; proving it continuously turns a claim into a public artifact. `engine` `small`
- [ ] **Frame the benchmark in prompt-compression vocabulary (compression ratio × answer fidelity, cite LLMLingua/LongBench) and name the novel axis 'referent resolution under brief composition'** — Borrowed academic vocabulary makes the benchmark legible and searchable to the ML audience instead of reading as homegrown marketing. `docs` `small`
- [ ] **Cite ContextBranch (arXiv 2512.13914) in docs/pitch: 58% context reduction, +13% on distant explorations, with the honest near-topic caveat** — Independent evidence for the core thesis beats self-reported benchmarks and the caveat builds credibility. `docs` `small`
- [ ] **Positioning doc 'Routing you can read': inspectable open-source router + never-override-manual-picks vs the GPT-5 silent-router backlash** — Transparency is the documented number-one complaint about every closed router and Bonsai already has the architecture — it just hasn't claimed the position. `docs` `small`
- [ ] **Positioning post 'Compression at the boundary, not at the buffer': fork-time briefs vs overflow-time compaction, citing context-rot research and auto-compact complaints** — The strongest empirical validation of the pitch already exists in public complaint threads; nobody has framed it as a product distinction. `growth` `small`
- [ ] **Comment on claude-code #32631 demonstrating the plugin as the working implementation of the fork/merge/tree spec** — The issue's watchers are exactly Bonsai's target users, and being the reference implementation is free distribution. `growth` `small`
- [ ] **Submit the plugin to the Anthropic community directory and skillrank.dev after adding repository/license/homepage/keywords to plugin.json** — The /plugin Discover tab and directory sites are where installs come from now, and screening confers a trust signal. `growth` `small`
- [ ] **Publish the plugin's own context overhead as a README badge ('Bonsai costs <2k tokens of your window')** — Context bloat is the #1 plugin complaint of 2026 and a token-thrift plugin must be demonstrably light itself. `docs` `small`
- [ ] **SECURITY section in the README (no network calls, no telemetry, dependency-free stdio server, local tree) plus SHA-pinned marketplace releases** — Post-CVE 2026 users and directory reviewers treat plugins as trusted code; a local-only auditable server is a differentiator worth stating. `docs` `small`
- [ ] **Plugin statusline segment: 'bonsai: 12.4k pruned this session' ticker from the tree store** — The ccusage-monitor ecosystem shows devs want ambient always-visible cost state — the retention mechanic that made caveman and claude-mem sticky. `plugin` `small`
- [ ] **SessionStart hook: when the project has unmerged branches, inject one line ('bonsai: 2 open branches — /bonsai:tree')** — Cross-session continuity is claude-mem's whole value; Bonsai already persists the tree per-project but never resurfaces it. `plugin` `medium`
- [ ] **'Widen' fast path: when a branch punts with 'brief does not cover', auto-propose the missing facts for a one-keystroke re-fork** — The coverage-gate punt is the plugin's most friction-prone moment; a guided retry keeps the loop trustworthy. `plugin` `medium`
- [ ] **'Branch these:' fan-out — multiple side questions forked and run as parallel tier agents, each INSIGHT merged with a combined economics line** — Parallel subagents are a headline 2026 pattern and the brief compiler makes N branches affordable where full-copy forks are not. `plugin` `medium`
- [ ] **Background-branch completion surfacing via Stop-hook/task notification so the merge INSIGHT lands without the user asking** — Background branches currently complete silently; a top-tier plugin closes its own loop. `plugin` `medium`
- [ ] **OAuth handshake debug log with a /connect/debug page showing where the flow died** — claude.ai connector OAuth has documented silent-failure modes; a handshake trace is the only way to debug them without guessing. `connector` `medium`
- [ ] **Serve both root-level and path-scoped OAuth well-known documents with identical content** — Real clients disagree on which discovery path they hit; serving both eliminates a documented class of connection failures. `connector` `small`
- [ ] **Scope tokens per capability: read (tree/export) vs write (fork/merge/abandon), enforced per-tool** — 'Bearer token grants everything' is the ecosystem critique; per-tool scopes are cheap with 4 tools and a differentiator in security review. `connector` `small`
- [ ] **Post-deploy smoke test replaying claude.ai's exact handshake (PRM, DCR, PKCE, initialize, tools/list, one call) against prod** — Connector breakage historically arrives via claude.ai client updates; a replay distinguishes Bonsai regressions from Anthropic-side bugs immediately. `connector` `medium`
- [ ] **CIMD readiness: accept HTTPS-URL client_ids, fetch/cache metadata, validate redirect_uris, alongside DCR** — The 2026-07-28 MCP spec deprecates DCR for Client ID Metadata Documents; supporting both future-proofs the connector. `connector` `medium`
- [ ] **Public connector docs page: install-in-claude.ai in 3 steps, example fork/merge prompts, the one-insight merge contract** — Required for directory launch and doubles as the landing page for strangers arriving via the registry. `docs` `small`
- [ ] **Standalone npm-facing engine README: 5-line compileBrief/route/merge example, zero-dependency claim up top, verified via npm pack --dry-run** — npmjs.com renders the package README as the landing page for the install-first audience and it must stand alone from the monorepo story. `engine` `small`
- [ ] **Preview-with-key shareable link: 'try it on real models' button routing to the auth-gated preview with an expiring share link** — Closes the gap where every public visitor only ever sees the extractive mock, without shipping the API key on production. `web-app` `medium`
- [ ] **Shareable pruning card: one-click PNG/SVG (terminal aesthetic, 'X tokens never sent · $Y avoided', tree silhouette, watermark)** — ccusage went viral on screenshot-shaped output; a canonical card turns every satisfied user into distribution. `growth` `medium`
- [ ] **Technical deep-dive post 'How we compile a 19k-token conversation into a 400-token brief that survives three forks', ending with the benchmark table** — HN launches with a substantive technical artifact behind them sustain front-page time far longer than a bare repo link. `growth` `medium`

## P3 — moonshots / later

_Big swings, revisit after P0-P1._

- [ ] **Beam-lite compare fork: run one brief at two router rungs side-by-side, pick the winner, feed the pick into the flywheel as preference data** — Steals big-AGI Beam's best interaction while generating exactly the labeled data the learned router needs. `engine` `large`
- [ ] **Shadow-evaluation sampling: on opt-in N% of routed turns, also run the tier above in background, LLM-judge both, feed win/loss into priors** — Solves the counterfactual blindness every production router has — the router learns when cheap picks actually lose, not just when users complain. `engine` `large`
- [ ] **Offline router eval harness (RouterBench-style): precompute all-tier answers on ~200 questions, judge once, replay any policy free, publish the cost-quality Pareto curve and a 'Bonsai vs always-frontier' page** — Every router markets one number (RouteLLM 85%, FrugalGPT 98%); Bonsai has none, and brief savings stack on routing savings — a claim no competitor can make. `engine` `large`
- [ ] **Local zero-dependency classifier for the routing cold path (vLLM Semantic Router pattern), LLM classifier only for low-confidence cases** — A classifier that costs an LLM call per message eats the savings it creates; tiny local classifiers handle the bulk of intent routing. `engine` `large`
- [ ] **Run the brief compiler against the public LongMemEval dataset and publish scores vs full-history and naive-summary baselines** — An external, third-party-rerunnable number beats the field's self-reported benchmarks and is a marketing asset for the moat claim. `engine` `large`
- [ ] **Plugin session-replay benchmark: 10 recorded sessions comparing compiled-brief branch vs /fork full copy, tokens/cost/quality per tier** — The 'typically 95%+ pruned' claim needs receipts to survive the skeptical Reddit/HN scrutiny that made or broke 2026 plugins. `engine` `large`
- [ ] **Cross-surface garden sync: the plugin's MCP server optionally pushes its tree to a connector garden key so one garden spans terminal and claude.ai** — MOAT.md item 3 (workflow-integration depth) and a concrete first answer to the open durable-memory question without building a full memory layer. `engine` `large`
- [ ] **claude.ai hidden-branch importer: ingest a conversation's invisible edit-forks and render them in the garden with orphaned-branch detection** — The 'Claude lost my chat on an invisible branch' incident shows real pain; three Chrome extensions exist just to draw this tree and none can fork/merge from it. `connector` `large`
- [ ] **Tree-as-cost-map: size/heat every garden node by actual spend with the pruned counterfactual as a ghosted halo** — Langfuse's trace tree is its best-loved surface; Bonsai's conversation tree can BE the cost visualization, which no observability tool can copy. `web-app` `large`
- [ ] **Session-replay mode: scrubber replaying tree growth event-by-event with grow/merge animations and a running saved-tokens counter** — Turns any real session into a self-narrating demo of briefs, routing, and merge economics — ideal for the landing page and launch video. `growth` `large`
- [ ] **bonsai-usage CLI (npx): parse local session logs into a ccusage-style sent-vs-pruned table, no telemetry, --share emits the pruning card** — Rides the exact distribution channel and trust model (local logs, no upload) that made ccusage a 16.5k-star meme. `growth` `medium`
- [ ] **Public 'pruned leaderboard' (viberank inversion): opt-in card submissions ranking developers by tokens NOT sent** — Tokenmaxxing leaderboards formed spontaneously around spend; a savings leaderboard is the unclaimed inversion and pure earned media for the thesis. `growth` `medium`
- [ ] **/diff two branches: LLM comparison of sibling branches' conclusions with a recommendation, billed at the cheapest adequate rung** — Spec'd as aspirational in #32631 and shipped by nobody — a visible capability gap Bonsai can own and a natural router showcase. `web-app` `medium`

